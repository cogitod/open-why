# Paths exempt from the UUID-density check in hooks/check-leaks.sh (never from the
# secret-pattern check — a real secret must be removed/revoked, not allowlisted).
# One path per line. Add a comment above each entry explaining why it's safe.
