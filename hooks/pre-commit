#!/usr/bin/env bash
# Activate with: git config core.hooksPath hooks
set -euo pipefail
root="$(git rev-parse --show-toplevel)"
bash "$root/scripts/check-rust-loc.sh" staged
exec "$root/hooks/check-leaks.sh" staged
